{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'cd /app && PYTHONPATH=/app python -m alembic upgrade head && PYTHONPATH=/app python scripts/seed.py; PYTHONPATH=/app celery -A app.worker worker -Q simulation --loglevel=info --concurrency=2 & uvicorn app.main:app --host 0.0.0.0 --port $PORT'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
